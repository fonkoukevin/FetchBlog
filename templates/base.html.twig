<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
              integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
 <style>

     .nav-link.active {
         color: #2f5b74;
         /* Votre style personnalisé ici */
         /* Par exemple, pour supprimer la bordure bleue */
         /* border: none; */
         /* Ou pour ajouter une autre couleur de fond */
         background-color: #f5f5f5;
         color: white;
         /* Ou toute autre propriété que vous souhaitez modifier */
         /* margin : 20px auto 60px; */
         position: relative;
         /* le relative est utilise quand on a un ::after,::before pour que les element qui herite aurons une postion relative */
         line-height: 24px;

     }

     .nav-link.active::after {
         background-color: #f5f5f5;
         color: white;
         content: '';
         background: #2f5b74;
         width: 15px;
         height: 5px;
         border-radius: 5px;
         position: absolute;
         bottom: 5px;
         left: 50%;
         transform: translateX(-50%);
     }

     .log{
         margin-bottom: 10px;
         margin-right: 7px;
         width: 35px;
         border-radius: 5px;
         height: auto;
     }

     @font-face {
         font-family: "Roboto";
         src: url('../public/fonts/Roboto/Roboto-Condensed.ttf');
         font-weight: normal;
         font-style: normal;
     }
     @font-face {
         font-family: "libreFranklin";
         src: url('../public/fonts/Libre_Franklin/LibreFranklin-VariableFont_wght.ttf');
         font-weight: normal;
         font-style: normal;
     }

     .rob{
         font-family: "Roboto";
     }
     .librefrank{
         font-family: "libreFranklin";
     }
 </style>


        <header>
            <section class="navigation mb-5">
                <div class="container-fluid">
                    <div class="row">
                        <nav class="navbar navbar-expand-lg bg-body-tertiary">
                            <div class="container-fluid">
                                <a class="navbar-brand ms-5" href="{{ url("home") }}"><img src="{{ asset('images/logo2.png') }}" alt="handshake"
                                                                                           class="img-fluid log">FetchBlog</a>

                                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                                        data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                                        aria-expanded="false" aria-label="Toggle navigation">
                                    <span class="navbar-toggler-icon"></span>
                                </button>
                                <div class="collapse navbar-collapse me-5" id="navbarSupportedContent">
                                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 nav nav-pills">
                                        <li class="nav-item">
                                            <a class="nav-link {{ app.current_route == "home" ? 'active': '' }}" aria-current="page" href="{{ url("home") }}" style="background: none">Accueil</a>

                                        </li>
                                        <li class="nav-item">
                                            <a class="librefrank nav-link {{ app.current_route starts with 'post'? 'active' :''}}" href="{{ path("posts") }}" style="background: none">Posts</a>

                                        </li>
                                        <li class="nav-item">
                                            <a class="librefrank nav-link {{ app.current_route starts with 'communaute'? 'active' :''}}" href="{{ path("communautes") }}" style="background: none">Communauté</a>

                                        </li>
                                        <li class="nav-item">
                                            <a class="librefrank nav-link {{ app.current_route starts with 'apropos'? 'active' :''}}" href="{{ url("apropos") }}" style="background: none">A propos</a>

                                        </li>
{#                                        <li class="nav-item">#}
{#                                            <a class="nav-link" href="{{ url("app_logout") }}"><i#}
{#                                                        class="fa-solid fa-right-from-bracket"></i></a>#}
{#                                        </li>#}

                                    </ul>

                                </div>
                            </div>
                        </nav>
                    </div>
                </div>
            </section>
        </header>


        {% block body %}{% endblock %}

    </body>
</html>
